# Database Secret fixture for sensitive data testing
# This Secret contains base64-encoded database credentials
# Used to test values resolution from Secrets and mixing ConfigMaps with Secrets
# All values in data section are base64-encoded per Kubernetes Secret spec
apiVersion: v1
kind: Secret
metadata:
  name: database-credentials
  namespace: default
type: Opaque
data:
  # values.yaml: base64-encoded YAML content
  # The content below is base64 encoding of:
  # db:
  #   username: postgres
  #   password: secret123
  #   host: db.internal
  #   port: 5432
  values.yaml: ZGI6CiAgdXNlcm5hbWU6IHBvc3RncmVzCiAgcGFzc3dvcmQ6IHNlY3JldDEyMwogIGhvc3Q6IGRiLmludGVybmFsCiAgcG9ydDogNTQzMgo=
